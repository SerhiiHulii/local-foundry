{
  "name": "local-foundry",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "exec": "docker exec -it foundryvtt_12 /bin/bash",
    "start": "docker-compose -f docker-compose.yml up -d",
    "stop": "docker-compose -f docker-compose.yml stop && docker ps",
    "kill": "docker container rm $(docker container ps -a --format '{{.ID}}')",
    "prune": "docker system prune -a"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SerhiiHulii/local-foundry.git"
  },
  "private": true
}
